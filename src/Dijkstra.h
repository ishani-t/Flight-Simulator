#pragma once
#include "Flights.h"
#include "Edge.h"
#include "Node.h"

#include <vector>
#include <list>
#include <string>
#include <map>

using std::vector;
/*
class Dijkstra {
    public:
        Dijkstra(Flights graph, string start, string destination);
        void solveDijkstra();

    private:
        Flights graph_;
        Node start_airport_;
        Node dest_airport_;
        
        vector<Node> nodes_;
        std::map<string, double> distances_;
        vector<bool> visited_;

}*/